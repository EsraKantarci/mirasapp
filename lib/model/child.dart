class Child {
  String childName;
  String parentName;
 // int parentId;
  // int id;
  bool isAlive;

  Child(
      {this.childName = "",
      this.parentName = "",
     // this.parentId = -1,
     // this.id = -1,
      this.isAlive = true});
  //TO-DO: Add constructor
  //Child({this.childName = '', this.parentName = ''});
}
